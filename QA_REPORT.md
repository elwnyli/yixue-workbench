# 译学工作台 V2 QA 报告

更新时间：2026-08-20  
分支：`v2`

## 测试范围

本报告随 V2 功能迭代持续更新。当前覆盖数据模型、旧版 V2 迁移、IndexedDB 备份、安全导出、基础文件解析、应用首页加载，以及 AI 批量预翻译任务核心。

## 自动测试结果

| 测试项目 | 操作步骤 | 预期结果 | 实际结果 | 状态 | 错误日志 |
|---|---|---|---|---|---|
| 首页加载 | 清空测试数据库并渲染应用 | 从演示工作区进入首页 | 首页标题、快速翻译入口和数据边界正常显示 | 通过 | 无 |
| 快速翻译自动保存 | 输入非空原文并等待防抖保存 | 内容写入IndexedDB并可重新读取 | 刷新恢复数据与输入一致 | 通过 | 无 |
| 旧 V2 数据迁移 | 删除旧数据中的新字段后运行迁移器 | 保留项目、文件、片段及其 ID，并补齐新字段 | ID 保留，schemaVersion 升至 3 | 通过 | 无 |
| 新闻元数据迁移 | 迁移旧版 NewsItem | 补齐内容类型和版权状态，不伪造正文 | `paragraphs` 保持为空，版权状态为 metadata-only | 通过 | 无 |
| 非 V2 数据拒绝 | 输入 version=1 的对象 | 明确拒绝，不静默覆盖 | 返回可识别错误 | 通过 | 无 |
| IndexedDB 保存恢复 | 修改学习者名称、保存并重新加载 | 修改内容恢复 | 恢复成功 | 通过 | 无 |
| 迁移前备份 | 建立旧版数据库并加载 | 迁移前写入独立备份区 | 生成一条 migration 备份 | 通过 | 无 |
| 备份 object store | 首次打开数据库 | 自动创建 workspace-backups | object store 存在 | 通过 | 无 |
| 安全导出 | 在嵌套数据中加入 apiKey、secret | 敏感字段不得进入 JSON | 敏感值被移除，普通字段保留 | 通过 | 无 |
| 工作区导入解析 | 导入包装格式和原始 Workspace JSON | 都经过迁移层并生成当前结构 | 两种格式均成功 | 通过 | 无 |
| 粘贴文本切分 | 输入英文句子和中文段落 | 生成唯一、有序片段 | 生成3个片段，ID唯一 | 通过 | 无 |
| TXT/Markdown 导入 | 创建浏览器 File 并调用解析器 | 本地读取并识别文件类型 | TXT、Markdown 均成功 | 通过 | 无 |
| 批量任务范围保护 | 创建含人工译文和已确认片段的项目 | 默认只排队未翻译片段 | 人工译文与已确认片段均未进入任务 | 通过 | 无 |
| 片段 ID 回填 | 模拟服务逆序返回两个译文 | 按 segmentId 回填，不按数组顺序 | 两个片段均回填到正确位置 | 通过 | 无 |
| 翻译记忆优先 | 为待译片段建立完全匹配 | 先使用 TM，只把其余片段发给模型 | 完全匹配来自 TM，AI 请求减少一个片段 | 通过 | 无 |
| 单片段失败隔离 | 模拟服务漏回一个片段 | 其他片段成功，漏回片段单独失败 | 成功片段正常保存，失败片段可重试 | 通过 | 无 |
| 分批续跑 | batchSize 设为 1，连续执行两步 | 每步只处理一批并可恢复进度 | 首步 running，第二步 completed | 通过 | 无 |
| 请求期间人工编辑保护 | 请求完成前修改某片段译文 | 后返回的 AI 结果不能覆盖新人工译文 | 人工译文保留，其余片段正常应用 | 通过 | 无 |
| 模糊匹配 | 比较含少量差异的中文句段 | 返回可用于 TM 排序的相似度 | 相似度超过阈值 | 通过 | 无 |

自动测试汇总：前端 7 个测试文件、28 项测试，加安全代理 5 项测试，共 33 项全部通过。

## 人工验收记录

| 测试项目 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|
| 深色模式 | 设置页切换深色再切回浅色 | 根节点主题属性同步变化 | `dark` 与 `light` 正确切换 | 通过 |
| 项目搜索 | 输入不存在的项目名称 | 列表变为空并说明下一步 | 显示“没有符合条件的项目” | 通过 |
| 项目筛选 | 选择项目状态 | 仅保留对应状态项目 | 原生状态筛选已接入 | 通过 |
| 新闻刷新失败 | 未配置内容代理时点击刷新 | 不伪造新闻，显示可操作错误 | 显示“尚未配置内容同步代理” | 通过 |
| 新闻阅读边界 | 打开 metadata-only 内容 | 显示摘要、原链接和版权说明，不显示伪造正文 | 阅读抽屉符合边界 | 通过 |
| 语言资产新增 | 新增术语并搜索 | 表单保存，列表和IndexedDB可恢复 | 新术语可检索且已持久保存 | 通过 |
| Skill执行日志 | 运行“翻译质量分析”再打开日志 | 写入输入、结果、时间与状态 | 生成3条问题并保存执行日志 | 通过 |
| 五种复习模式 | 切换完形填空和重新翻译 | 提示与作答控件不同 | 完形显示挖空，重新翻译显示独立输入区 | 通过 |
| 编辑器搜索 | 输入原文关键词 | 只显示匹配片段 | 3个片段缩减为1个 | 通过 |
| AI批量预翻译 | 在演示项目点击“当前文件” | 分批执行、显示进度并保留AI原稿 | 2个未译片段完成，按片段ID显示AI原稿 | 通过 |
| 质量问题解决 | 运行检查后点击“标记已解决” | 问题写入解决状态并从待处理列表移除 | 状态更新逻辑和持久化已接入 | 通过 |
| 浏览器运行错误 | 完成上述操作后读取警告与错误日志 | 无未处理前端错误 | 日志为空 | 通过 |
| 代理密钥隔离 | 使用测试密钥运行连接动作 | 响应和日志不能包含密钥 | 响应中不存在测试密钥 | 通过 |
| 代理片段ID校验 | 模拟重复、未知和逆序片段结果 | 仅返回请求中的唯一片段ID | 未知与重复ID被移除 | 通过 |
| 代理来源诚信 | 模拟术语解释返回伪造网址 | 无检索能力时不得返回来源 | sources 被强制置空 | 通过 |
| 代理操作白名单 | 请求未支持的操作 | 在调用模型前拒绝 | 返回“不支持的操作” | 通过 |

## 截图

已对桌面端编辑器进行截图验收：批量任务进度、片段来源、AI原稿、当前译文和右侧辅助区可以同时辨识。截图暂保存在本次 Codex 验收会话中，发布前未向仓库写入临时浏览器图片。

## 已知限制

- AI批量预翻译核心和编辑器任务证据轨已实现；安全代理的实际服务端仍需用户自行部署和配置。
- 项目编辑器已可请求片段级参考与反馈；演示模式只返回固定示例，代理失败会明确回退并记入日志。
- 新闻阅读与选段练习已实现；实时 RSS/API 获取仍依赖用户提供的安全内容代理。
- 文化概念识别与新闻自动选段保持停用，直到安全代理、来源检索与版权判断接口可用。
- DeepSeek 安全代理已实现，但真实连通测试仍需用户在部署平台私密设置 `DEEPSEEK_API_KEY`；Key 不得发送到聊天或写入仓库。
- 语言资产已完成本地 CRUD 与搜索；当前库的批量 JSON 导入界面仍未加入，工作区整体导入可用。
- DOCX目前提取纯文本，尚未完整保留标题和列表结构。
